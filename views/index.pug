extends layout

block content
  main
    .title
      h2 Messages
    .msg-grid
      each el in messages 
        .msg
          if user !== undefined
            .msg-header
              span= el.user
              |
              | on #{ el.added }
          .msg-content= el.text
